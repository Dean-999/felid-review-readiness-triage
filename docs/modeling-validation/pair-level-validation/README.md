# Pair-Level Evidence Reliability Modeling

> **Historical v1 validation archive.** The Phase18M records and derived 400-row analyses are `v1_exploratory_not_confirmatory` because the historical reviewer interface exposed PF-ERI and identity-group information. They are preserved for provenance and design, not used as a PF-ERI v2 calibration or confirmation source.

Date: 2026-07-07

This layer contains the evidence that justifies moving from validation into
final PF-ERI modeling. The active modeling entry point is now the physical final
freeze:

```text
data/frozen/pferi_v2/
```

Do not start final modeling from old `outputs/phase16`, `outputs/phase17`, or
`outputs/phase18` candidate-selection folders. Those are provenance and
diagnostics. The current bootstrap contract is generated by:

```text
scripts/build_final_modeling_bootstrap.py
archive/pferi_v1/outputs/modeling-validation/final-modeling-bootstrap/
```

## Locked Scientific Scope

The project direction is:

```text
strong descriptor retrieval
-> candidate pair queue
-> PF-ERI pair-level evidence admissibility
-> descriptor-evidence conflict and evidence risk
-> calibrated review routing
```

PF-ERI is evaluated as a post-retrieval evidence-governance layer. It is not a
descriptor replacement, not a Bobcat identity classifier, and not a simple
pre-Re-ID photo filter.

## Current Frozen Inputs

| Scope | Rows | Modeling role | Identity validation |
| --- | ---: | --- | --- |
| `lynx-wild` | 3,000 | known-ID CzechLynx validation core | allowed |
| `bobcat-wild` | 3,000 | wild Bobcat transfer/evidence stress | blocked |
| `bobcat-urban` | 6,000 | urban/peri-urban Bobcat review-risk stress | blocked |
| `lynx-urban` | auxiliary only | small heterogeneity note | blocked |

Bobcat rows do not currently provide verified individual identity labels. They
can support review-readiness, pair comparability, descriptor-evidence conflict,
and domain/stress analysis, but not Bobcat identity accuracy.

## Phase18 Evidence Base

Phase18 established the core mechanism under strong descriptor controls.

### Descriptor-Controlled Confirmation

Phase18L tested high descriptor-similarity pairs while contrasting high and low
PF-ERI admissibility.

Current result:

```text
PASS
```

Summary:

- MegaDescriptor: strong support.
- DINOv2: directionally consistent in Phase18L.
- Pooled: support.

Interpretation:

```text
PF-ERI adds pair-level evidence information beyond generic image-quality scores
and descriptor similarity.
```

### Identity-Balanced Confirmation

Phase18M controlled descriptor family, descriptor similarity, and identity
stratum:

```text
same-ID + high PF-ERI admissibility
same-ID + low PF-ERI admissibility
different-ID + high PF-ERI admissibility
different-ID + low PF-ERI admissibility
```

Status:

```text
BLIND_CONFIRMED_IDENTITY_BALANCED_PASS
```

Majority-vote effects:

| Descriptor | Identity stratum | High uncertain/not-ready | Low uncertain/not-ready | Difference |
| --- | --- | ---: | ---: | ---: |
| MegaDescriptor | same-ID | 2% | 18% | +16% |
| MegaDescriptor | different-ID | 20% | 64% | +44% |
| DINOv2 | same-ID | 2% | 40% | +38% |
| DINOv2 | different-ID | 30% | 74% | +44% |

Reviewer reliability:

```text
400 pairs
3 reviewers
1200 labels
minimum binary kappa = 0.423
```

Interpretation:

```text
Low PF-ERI admissibility remains enriched for human uncertain/not-ready
reviewability labels after descriptor family, descriptor similarity, and known
same/different identity stratum are controlled.
```

## Strong Descriptor Boundary

Strong descriptors are upstream candidate generators and controls. Current
project language may refer to:

```text
megadescriptor_l_384
dinov2_vitl14
```

PF-ERI should be compared against descriptor-only, quality-only, PF-ERI-only,
and descriptor + PF-ERI models. The claim is not that PF-ERI replaces those
descriptors. The claim is that PF-ERI adds pair-level reviewability and
evidence-risk governance after retrieval.

## Selective Evidence Sufficiency Modeling Plan

The next algorithmic work is the PF-ERI Selective Evidence Sufficiency Model:

```text
A risk-calibrated selective inference layer for wildlife Re-ID candidate pairs
```

The work is organized by module purpose rather than new phase numbers:

```text
modeling-contract:
  build the final pair-level input contract from data/frozen/pferi_v2

evidence-feature-extraction:
  compute image evidence, pair comparability, descriptor conflict, and
  domain/source stress features

known-id-evidence-sufficiency-validation:
  train and validate the evidence sufficiency model on known-ID CzechLynx pairs

risk-calibrated-evidence-admission:
  calibrate selective-risk thresholds and report risk-coverage behavior

evidence-risk-decomposition:
  explain not-ready risk by observable evidence components and reason labels

bobcat-wild-urban-transfer-stress:
  evaluate evidence-risk shift and review burden under Bobcat wild/urban data
  without identity-accuracy claims

review-budget-routing:
  select pair subsets under fixed review budget and target evidence-risk levels

robustness-and-claim-gates:
  run group-aware splits, descriptor-family stratification, ablations,
  calibration checks, and blocked-claim audits
```

The central mathematical target is:

```text
maximize accepted-pair coverage
subject to calibrated selective evidence risk <= alpha
```

Primary model comparisons:

```text
descriptor-only
quality-only
PF-ERI evidence-only
descriptor + PF-ERI
descriptor + PF-ERI + selective calibration
```

## Claim Boundary

Allowed:

- pair-level evidence admissibility;
- review-readiness;
- evidence-risk routing;
- descriptor-evidence conflict;
- CzechLynx known-ID validation;
- Bobcat wild/urban transfer and stress analysis.

Blocked unless new labels are added:

- Bobcat individual identification accuracy;
- Bobcat false-match accuracy;
- descriptor-training improvement as the main contribution;
- claims that `lynx-urban` is a required or complete 3,000-image modeling cell.

# Robust Local-Match v2 Kaggle Guide

Upload `final_local_match_package_v2.zip` and the opaque pilot input ZIP as
separate Kaggle Datasets. Enable Internet and GPU. Run the packaged notebook
without edits: install, freeze, run, checker, output ZIP. Return
`PF_ERI_FINAL_RESULTS.zip` unchanged. This is an outcome-free v2 feasibility
run; it does not overwrite the detector-gated v1 technical-failure record.

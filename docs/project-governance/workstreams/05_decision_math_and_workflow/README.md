# Workstream 05: Decision Mathematics and Workflow Validity

Status: complete — PF-ERI v2 is `CONFIRMED` and closed at the owner-defined Task15M execution-validation acceptance gate. The run audit passed, the validation audit passed with no failures, the task chain is closed, and there is no next Task. This operational confirmation does not authorize identity-accuracy or automatic identity-assignment claims.
Primary dependency: Workstream 04 must be `v2_design_locked` before confirmation analysis.
Exit dependency: Workstream 06 may use only a confirmed and bounded core interface.

## Purpose

This workstream establishes the project’s applied-mathematics contribution without turning PF-ERI into a collection of fashionable algorithms. The model estimates the probability that a pair is review-ready. The decision problem then uses that probability to decide whether to admit the pair, send it to expert review, or defer it. The scientific question is whether this decision improves a specified proper-score and workflow objective beyond descriptor similarity and independent image-quality controls.

## Procedure

Before confirmation labels are opened, freeze the interpretable regularized probability model, its transformations, the independent-quality active control, the automatic pair-evidence inputs that passed Workstream 02, the calibration procedure, and the cluster-aware uncertainty method. The only primary incremental comparison is the full model against descriptor similarity plus independent image quality on identical confirmation rows. The primary measure is change in Brier score, accompanied by its graph-aware 95% interval and the pre-specified minimum practical increment. AUROC, AUPRC, log loss, calibration displays, descriptor-specific estimates, nonlinear models, and oracle-feature comparisons remain secondary unless promoted before review begins.

Next, lock the three-action asymmetric loss for `admit`, `expert_review`, and `defer`. It must count incorrect admission, unnecessary review, unnecessary defer, automatic inference time, feature failure, manual correction where used, and human-review minutes. The project reports transparent relative-cost sensitivity and observed time; it does not invent a conservation monetary value. A claim of conformal or distribution-free risk control is forbidden unless the required calibration split, loss definition, coverage meaning, assumptions, and finite-sample guarantee are actually satisfied.

Finally, run a consequential-validity study on a pre-specified deployment-queue subset. New blinded reviewers complete a known-identity same/different judgement with confidence and elapsed time, without generating the reviewability label or seeing routes. The analysis tests whether locked route states differ in human judgement reliability, confidence, or time. It connects the latent evidence-admission construct to a real human decision while preserving the boundary that PF-ERI itself does not assign identity.

## Required Artifacts and Exit Decision

The workstream produces a frozen modelling specification, a calibration record, action-loss specification, cost and budget sensitivity register, graph-aware inference plan, confirmation report, and consequential-validity analysis. Its exit decision is `v2_confirmed_with_boundaries`, `v2_mixed`, or `v2_not_confirmed`. A route may be called efficient only if the representative deployment sample shows its pre-specified benefit after full feature-acquisition and human-time costs are counted.

## Current task state

Task 01 is complete at `01_model_route_hv_literature_review_20260722.md`. Task 02 is complete at `02_model_route_candidate_registry_freeze_20260722.md`; it freezes the 14 candidate roles, the nested P5-versus-P3 primary comparison, complexity budgets, feature-block boundaries, and prohibited routes without fitting a model or reading a locked-stage outcome.

Task 03 is complete at `03_outcome_free_design_simulation_20260722.md`. Its corrected three-stage simulation used all 28,295 outcome-free within-role pairs, trained on 445 synthetic-development rows, independently recalibrated on 445 synthetic-calibration rows, and evaluated on the 9,417-row confirmation-role covariate population. Under the frozen qualification-before-minimax rule, square-root inverse-probability weighting was the only ridge strategy to qualify, but its calibration margin was narrow; this is a conditional design choice, not an observed-performance claim. Its required component-disjoint-fold gate is recorded separately in Task 04.

Task 04 is complete at `04_component_disjoint_nested_folds_20260722.md`. The 445 development pairs form 116 endpoint-image components; the largest contains 68 pairs. The frozen five-fold outer and conditional four-fold inner assignments have zero endpoint leakage, exact deterministic reproduction, outer and inner validation sizes of 88–91 pairs, and complete descriptor-support coverage. Only two matcher-failure pairs exist, so universal rare-level support is mathematically impossible and remains an explicit downstream limitation.

Task 05 is complete at `05_feature_preprocessing_freeze_20260722.md`. It freezes the ten permitted source predictors, training-fold median/scaling rules, explicit missing and unknown states, transformer serialization, and exact P3/P5 column nesting. The canonical fixture yields 20 P3 and 25 P5 transformed columns; all five additions are pair-evidence columns. The freeze executable read no development label value and fitted no predictive model. The next authorized task is Task 15E: component-disjoint ridge primary and restricted-GAM development comparison. Locked calibration and confirmation outcomes remain inaccessible.

Task 15E is complete and frozen at `07_task15e_development_model_comparison_freeze_20260722.md`. Its ModelScope CPU run produced all 3,560 expected out-of-fold predictions and passed both upstream and independent integrity checks. P5 reduced weighted Brier relative to P3 by 0.00004117, but this is only 0.0294% of P3 Brier, 94.7% of the global improvement arose from one fold, and the selected ridge penalty swung between 0.1 and 100. P5 therefore did not qualify as the final route. Restricted nonlinear models showed a stronger overall development signal, but their incremental pair-evidence contrast remained weak. Task 15E is immutable; the next authorized task is Task 15F Bayesian/Firth mathematical sensitivity, with locked-stage outcomes remaining inaccessible.

Task 15F-A is complete at `08_task15f_bayesian_sensitivity_contract_freeze_20260722.md`. It freezes S3 weak-prior Bayesian logistic, S4 non-centered crossed endpoint-image random effects, population-marginal prediction for two unseen endpoints through 20-node Gauss–Hermite quadrature, PyMC 6.0.1 NUTS settings, convergence gates, prior-predictive checks, and the S5 separation trigger. The audit returned `TRIGGERED_DIAGNOSTIC_ONLY` in outer folds 0 and 4, without complete-separation LP feasibility. S5 must therefore be run as a labelled Firth/FLIC diagnostic, but cannot participate in final route selection because its numeric thresholds were recorded after development outcomes opened. The next authorized action is the exact-contract Task 15F ModelScope CPU package; locked-stage outcomes remain inaccessible.

Task 15F-B is complete at `09_task15f_modelscope_cpu_package_20260722.md`. The immutable package contains the exact S3/S4/S5 execution, mandatory prior-predictive smoke gate, hashed fold-level resume checkpoints, accepted-posterior retention, global result validation, and one-file export builder. Exact Python/PyMC/nutpie and data-science dependency versions are pinned; Pandas remains at 2.3.3 to preserve the SHA-frozen preprocessing implementation. Local exact-environment smoke and short non-scientific S3/S4 interface tests passed. The next authorized action is the full external CPU run and return of the validated final export; locked-stage outcomes remain inaccessible.

Task 15F-C is complete and frozen at `10_task15f_bayesian_sensitivity_result_freeze_20260723.md`. The returned 55-member export, 54 internal checksums, 890 out-of-fold predictions, ten posterior fits, five S4 marginalization records, and S5 diagnostic all passed upstream and independent verification. All posterior fits passed on their first attempt with zero divergences and maximum R-hat below 1.005. S3 and S4 improved development proper scores descriptively relative to P5, but neither dominated across Brier score, log loss, and outer folds. Local-match coverage had a positive posterior mean in all ten model-by-fold summaries, while every 95% credible interval crossed zero. Shared endpoint-image heterogeneity was material, but S4 did not achieve a stable Brier advantage. S3, S4, and S5 remain non-confirmation-eligible sensitivities; no final model was selected. Task 15G is the next authorized task, while calibration and both confirmation stages remain locked.

Task 15G-A is complete and frozen at `11_task15g_exploratory_performance_bound_contract_freeze_20260723.md`. It fixes eight no-interaction EBM configurations and twelve deterministic shallow CatBoost configurations under the same fold-training preprocessing, square-root IPW fitting, design-weighted validation, and component-disjoint nested folds. Fixed-version TabPFN is pinned to an exact package, repository revision, checkpoint, size, and SHA256, but its frozen API lacks native sample-weight support; E3 is therefore predeclared ineligible under the common weighting contract and cannot be approximated by duplication or resampling. The audit verified 445 development pairs, 116 components, 1,780 nested assignments, and all authoritative hashes without fitting a model or reading locked-stage outcomes. The next authorized action is to build the exact Task 15G-B execution package. Calibration and both confirmation stages remain locked.

Task 15G-B is complete at `12_task15g_modelscope_package_20260723.md`. Its immutable package binds the E1/E2 runner, all development inputs and folds, exact dependencies, frozen TabPFN wheel and checkpoint, E3 API audit, strict package inventory, fold-level resume checkpoints, global validation, and one-file export. A real Python 3.12 smoke fitted both eligible estimators successfully without mutating the package or accessing a locked outcome.

Task 15G-C is complete and frozen. `13_task15g_exploratory_performance_bound_result_freeze_20260723.md` records the exact local macOS reproduction, while `14_task15g_external_result_reconciliation_20260723.md` records the authoritative returned ModelScope export and cross-platform reconciliation. The external 53-member export, 52 internal checksums, 400 inner configuration-fold fits, ten outer refits, and 890 OOF predictions passed all upstream and independent checks. E2 had lower aggregate weighted Brier and log loss than E1, but E1 had lower Brier in three of five folds, lower loss for most pairs and components, and higher AUROC/AUPRC. Fold 2 supplied more than the complete net E2 advantage after other folds offset it. Both exploratory Brier scores remained above the frozen S3/S4 sensitivity scores. Linux and macOS selected identical configurations and produced the same scientific conclusion; only E1 showed small platform-level probability differences. E1, E2, and E3 remain non-confirmation-eligible, and no final model was selected. Task 15H is now the only authorized next task; calibration and both confirmation stages remain locked.

Task 15H is complete and frozen at `15_task15h_development_model_freeze_20260723.md`. Its source and record validations passed, but no model passed the complete qualification-before-performance rule. P5 retains its binding `NOT_QUALIFIED` disposition, P3 is not an automatic post-result fallback and shares the failed 1000-fold regularization swing, and the observed-development calibration gate remains unresolved because no numeric threshold was frozen before outcomes were opened. S1-S5 and E1-E3 remain confirmation-ineligible. The resulting `development_model_freeze_record.json` sets `development_model_frozen=false` and `calibration_authorized=false`; calibration and both confirmation stages remain locked pending a governance decision on redesign or termination.

Task 15I-A/B is complete and frozen at `16_task15i_independent_redevelopment_design_20260725.md`. It establishes an outcome-free independent redevelopment route without reading or reassigning any existing locked stage. It fixes lambda 100 for P3 and P5 through a registered synthetic minimax stress simulation, requires a future image-disjoint and pair-disjoint sample of at least 1,200 analyzable pairs in at least 300 endpoint-image components, and freezes prospective stability, increment, and calibration gates. The associated operating-characteristic calculation supports the development point-estimate screen under its declared synthetic assumptions; it does not provide confirmation power or authorize calibration. Task 15I-C must build and audit a genuinely independent redevelopment sampling frame before any new development outcome can open.

Task 15I conditional feasibility analysis is complete at `17_task15i_conditional_90pct_feasibility_20260725.md`. It distinguishes a conditional 90 percent development point-estimate screen from a conditional 90 percent confirmation-like lower-bound target. Given a synthetic true Brier increment of 0.01, SD 0.12, intracomponent correlation 0.10, and four pairs per component, 400 components and 1,600 pairs provide 0.9281 conditional screen success, while 2,000 components and 8,000 pairs are required for 0.9047 conditional lower-bound success. These are not estimates of actual pass probability and do not amend Task 15I-A/B, authorize new labels, or open calibration. An adopted 1,600-pair design would first require a separate prelabel contract amendment and a capacity audit of a genuinely new, zero-overlap photo reservoir.

Task 15I-C initial capacity audit is complete at `18_task15i_independent_redevelopment_capacity_audit_20260725.md`. The recorded external Eurasian lynx reservoir contains 4,765 distinct image URLs but zero image files at its registered download paths; it has no pair graph or new automatic measurement results. It therefore cannot establish image-content zero overlap, the 400-component/1,600-pair graph target, or measurement adequacy. The authoritative hash-verified audit is `NOT_READY`; no contract amendment, human outcome collection, calibration, or confirmation is authorized. The next task is controlled acquisition and content-hash deduplication of a genuinely new reservoir, followed by an outcome-free pair-graph and measurement audit.

Task 15I-C reservoir materialization is complete at `19_task15i_independent_reservoir_materialization_20260725.md`. The project materialized 4,139 explicitly Creative Commons licensed external images, retained 4,108 after content-SHA256 deduplication, and observed zero source-ID, URL, and file-content overlap with every existing frozen role. A 4,108-image SHA256-verified, image-only descriptor execution package is ready for external GPU inference. This clears only the image-reservoir prerequisite; candidate pairing and automatic measurement remain outstanding, so the capacity audit remains `NOT_READY` and no outcomes may open.

Task 15I's declared-manual-review development result is complete and frozen at `20_task15i_declared_manual_review_development_result_freeze_20260727.md`. The accepted result contains 3,200 eligible manual decisions over 1,600 pairs in 400 endpoint-disjoint components. Under the prospectively frozen five-fold screen, P5 improved weighted Brier over P3 by `0.0058628987`, with nonnegative increments in all five outer folds and zero endpoint leakage. This passes the independent development screen only; it is not an external-performance or identity-accuracy result.

Task 15J is complete and frozen at `21_task15j_independent_development_model_freeze_20260727.md`. It freezes the fixed-`lambda=100` P5 model and P3 active control without changing the earlier Task15H finding for the original 445-pair route. Task 15K then froze the independent calibration design at `22_task15k_independent_calibration_design_20260727.md`, and Task 15L completed controlled calibration collection and apparent probability calibration at `23_task15l_calibration_collection_20260727.md`. These steps authorize use of the frozen coefficients and calibration parameters; they do not establish independent confirmation performance.

Task 15M's independent confirmation design and external inference execution are complete. Task 15N freezes the authoritative execution result at `24_task15n_confirmation_execution_result_freeze_20260728.md` with status `PASS_TASK15M_EXECUTION_VALIDATION`: both execution audits pass, all 889 candidate pairs are partitioned into 252 supported and 637 unsupported pairs, 815 images and 252 local-match rows have no recorded execution failures, and all 504 expected P3/P5 calibrated predictions are present. No Task15J coefficient, Task15L calibration parameter, Brier score, or other outcome-performance quantity was recalculated. The earlier wrong-source confirmation analysis is superseded and must not be cited. This result establishes execution integrity and coverage only; it does not establish P5 superiority, external predictive performance, identity accuracy, deployment utility, or project completion. The next authorized action is Task16 final claim matrix, manuscript integration, and governance closeout within this boundary.

Task 16 is complete at `25_task16_final_claim_matrix_and_manuscript_integration_20260728.md`. It freezes nine evidence-to-claim dispositions and the status `TASK16_CLAIM_MATRIX_FROZEN_PERFORMANCE_UNCONFIRMED`. The historical v1 manuscript remains exploratory, while the separate v2 manuscript integration may report bounded development qualification, calibration completion, and Task15M execution validation. External P5 superiority, identity accuracy, deployment utility, and project completion remain unestablished. The Workstream 05 exit decision is `DEFERRED_PENDING_AUTHORIZED_CONFIRMATION_OUTCOME_ANALYSIS`.

Task 17 is complete at `26_task17_confirmation_outcome_analysis_20260728.md`. It linked the 252 supported Task15M pairs one-to-one to the accepted deployment-confirmation outcomes through the frozen restricted linkage, used the frozen Task15J models and Task15L calibration without refitting, and applied the predeclared Hajek-weighted dyadic cluster-robust primary analysis. P3 weighted Brier was `0.307245886`, P5 weighted Brier was `0.509115668`, and the P3-minus-P5 increment was `-0.201869782` with 95% interval `[-0.224671400, -0.179068163]`. Because the lower bound is not greater than the frozen `0.005` threshold, the primary confirmation fails. The Workstream 05 exit decision is `v2_not_confirmed`; P5 external superiority, deployment utility, and project completion claims are closed as unsupported for PF-ERI v2. No retuning, recalibration, pair replacement, or repeat confirmation analysis is authorized.

The terminal owner-defined project closure is recorded at `27_final_project_closure_confirmed_20260728.md`. It freezes `status=CONFIRMED`, `project_status=CLOSED`, `task_chain_closed=true`, and `next_task=null` on the basis of Task15M execution validation. The separately present outcome analysis remains preserved as provenance and is not the closure basis. No Task17 or later analysis is required for project acceptance or closure.
